import 'package:flame/components.dart';

class Waste extends PositionComponent {
  // Return true to make component display in debug mode even no ui render yet
  @override
  bool get debugMode => true;
}
